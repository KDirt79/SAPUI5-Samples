<mvc:View
    controllerName="fahmaih.samples.warehouse.controller.Delivery"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc"
    displayBlock="true">
    <Page title="{i18n>Delivery}" 
          class="sapUiResponsiveContentPadding"
          showNavButton="true"
		  navButtonPress="onNavBack"
          showHeader="true"
          enableScrolling="false">
        <Label text="{DelivNumb}" />
    </Page>
</mvc:View>